{
  "name": "outer-wilds-mods",
  "homepage": "https://outerwildsmods.com",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "run-build": "npm run build && next start",
    "deploy": "npm run build && gh-pages -t -d out",
    "build": "next build && next export && npm run generate-sitemap",
    "generate-sitemap": "node generate-sitemap",
    "prettier": "prettier --write"
  },
  "dependencies": {
    "axios": "^0.20.0",
    "next": "9.5.2",
    "react": "16.13.1",
    "react-dom": "16.13.1",
    "react-markdown": "^4.3.1",
    "sass": "^1.26.10"
  },
  "devDependencies": {
    "@types/node": "^14.6.2",
    "@types/react": "^16.9.48",
    "gh-pages": "^3.1.0",
    "globby": "^11.0.1",
    "prettier": "^2.1.1",
    "typescript": "^4.0.2"
  }
}
