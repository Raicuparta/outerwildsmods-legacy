{
  "name": "outer-wilds-mods",
  "homepage": "https://outerwilds.ricardolopes.dev",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "run-build": "npm run build && next start",
    "deploy": "npm run build && gh-pages -t -d out",
    "build": "next build && next export && npm run generate-sitemap",
    "generate-sitemap": "node generate-sitemap",
    "prettier": "prettier --write"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "next": "9.5.1",
    "react": "16.13.1",
    "react-dom": "16.13.1",
    "react-markdown": "^4.3.1",
    "sass": "^1.26.10"
  },
  "devDependencies": {
    "@types/node": "^14.0.27",
    "@types/react": "^16.9.44",
    "gh-pages": "^3.1.0",
    "globby": "^11.0.1",
    "prettier": "^2.0.5",
    "typescript": "^3.9.7"
  }
}
